#include<stdio.h>
#include<conio.h>
int main()
{
	int a ,b,a1 ;
	printf("please Enter 2 No : ");
	scanf("%d %d",&a ,&b);
	if(a !=b)
	{
		if(a<b)
		{
			printf("No is in accending order ");
		}
		else
		{
			printf("no is not in accending order ");
		}
				
	}
}
